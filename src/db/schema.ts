import { pgEnum,pgTable,text,timestamp,integer,jsonb,boolean,primaryKey,uniqueIndex,numeric } from "drizzle-orm/pg-core";
export const projectStatus=pgEnum("project_status",["draft","queued","processing","composing","completed","failed","cancelled"]); export const memberRole=pgEnum("member_role",["owner","admin","member"]);
const timestamps={createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()};
export const users=pgTable("users",{id:text("id").primaryKey(),email:text("email").notNull().unique(),name:text("name").notNull(),passwordHash:text("password_hash").notNull(),...timestamps});
export const organizations=pgTable("organizations",{id:text("id").primaryKey(),name:text("name").notNull(),vertical:text("vertical").default("real_estate").notNull(),...timestamps});
export const organizationMembers=pgTable("organization_members",{organizationId:text("organization_id").references(()=>organizations.id,{onDelete:"cascade"}).notNull(),userId:text("user_id").references(()=>users.id,{onDelete:"cascade"}).notNull(),role:memberRole().default("member").notNull() },t=>[primaryKey({columns:[t.organizationId,t.userId]})]);
export const brandKits=pgTable("brand_kits",{id:text("id").primaryKey(),organizationId:text("organization_id").references(()=>organizations.id,{onDelete:"cascade"}).notNull().unique(),logoUrl:text("logo_url"),primaryColor:text("primary_color").default("#6957df"),secondaryColor:text("secondary_color").default("#172033"),website:text("website"),phone:text("phone"),socialHandle:text("social_handle"),...timestamps});
export const verticalDefinitions=pgTable("vertical_definitions",{id:text("id").primaryKey(),slug:text("slug").notNull(),name:text("name").notNull(),version:integer("version").notNull(),formSchema:jsonb("form_schema").notNull(),active:boolean("active").default(true).notNull(),...timestamps},t=>[uniqueIndex("vertical_slug_version").on(t.slug,t.version)]);
export const videoTemplates=pgTable("video_templates",{id:text("id").primaryKey(),vertical:text("vertical").notNull(),slug:text("slug").notNull().unique(),name:text("name").notNull(),description:text("description"),thumbnailUrl:text("thumbnail_url"),configuration:jsonb("configuration").notNull(),active:boolean("active").default(true).notNull(),...timestamps});
export const videoProjects=pgTable("video_projects",{id:text("id").primaryKey(),organizationId:text("organization_id").references(()=>organizations.id).notNull(),userId:text("user_id").references(()=>users.id).notNull(),vertical:text("vertical").notNull(),objective:text("objective").notNull(),title:text("title").notNull(),status:projectStatus().default("draft").notNull(),inputData:jsonb("input_data").notNull(),renderPlan:jsonb("render_plan"),templateId:text("template_id").references(()=>videoTemplates.id),aspectRatio:text("aspect_ratio").default("9:16").notNull(),durationSeconds:integer("duration_seconds").default(30).notNull(),outputUrl:text("output_url"),thumbnailUrl:text("thumbnail_url"),errorMessage:text("error_message"),estimatedCost:numeric("estimated_cost"),actualCost:numeric("actual_cost"),...timestamps});
export const mediaAssets=pgTable("media_assets",{id:text("id").primaryKey(),projectId:text("project_id").references(()=>videoProjects.id,{onDelete:"cascade"}).notNull(),storageKey:text("storage_key").notNull(),url:text("url").notNull(),mediaType:text("media_type").notNull(),label:text("label"),position:integer("position").notNull(),metadata:jsonb("metadata").default({}).notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
export const generationJobs=pgTable("generation_jobs",{id:text("id").primaryKey(),projectId:text("project_id").references(()=>videoProjects.id,{onDelete:"cascade"}).notNull(),provider:text("provider").notNull(),providerPredictionId:text("provider_prediction_id"),jobType:text("job_type").notNull(),status:projectStatus().notNull(),input:jsonb("input").notNull(),output:jsonb("output"),errorMessage:text("error_message"),webhookEventId:text("webhook_event_id").unique(),startedAt:timestamp("started_at",{withTimezone:true}),completedAt:timestamp("completed_at",{withTimezone:true}),...timestamps});
export const creditAccounts=pgTable("credit_accounts",{organizationId:text("organization_id").primaryKey().references(()=>organizations.id),balance:integer("balance").default(100).notNull(),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()});
