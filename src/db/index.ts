import postgres from "postgres"; import {drizzle} from "drizzle-orm/postgres-js"; import * as schema from "./schema"; let singleton:ReturnType<typeof drizzle<typeof schema>>|undefined; export function getDb(){if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL doit être configurée côté serveur"); singleton??=drizzle(postgres(process.env.DATABASE_URL,{prepare:false}),{schema});return singleton}
