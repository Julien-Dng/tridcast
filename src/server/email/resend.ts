export type LoginEmail={to:string;code:string;expiresInMinutes:number};
export interface EmailSender{sendLoginEmail(input:LoginEmail):Promise<void>}
export function renderLoginEmail({code,expiresInMinutes}:LoginEmail){return `<!doctype html><html lang="fr"><body style="margin:0;background:#f7f8fc;font-family:Arial,sans-serif;color:#172033"><table role="presentation" width="100%"><tr><td align="center" style="padding:32px 16px"><table role="presentation" width="100%" style="max-width:520px;background:#fff;border:1px solid #e6e7ef;border-radius:20px"><tr><td style="padding:36px"><div style="font-size:24px;font-weight:800;color:#6957df">tridcast</div><h1 style="font-size:25px">Votre code de connexion</h1><p>Saisissez ce code pour continuer :</p><p style="font-size:34px;font-weight:800;letter-spacing:8px;background:#f3f1ff;padding:18px;text-align:center;border-radius:12px">${code}</p><p>Ce code expire dans ${expiresInMinutes} minutes et ne peut être utilisé qu’une fois.</p><p style="color:#667085;font-size:14px">Si vous n’avez pas demandé cette connexion, ignorez cet email. Ne communiquez jamais ce code.</p></td></tr></table></td></tr></table></body></html>`}
export class ResendEmailSender implements EmailSender{async sendLoginEmail(input:LoginEmail){const key=process.env.RESEND_API_KEY,from=process.env.AUTH_EMAIL_FROM;if(!key||!from)throw new Error("Envoi email indisponible : configurez RESEND_API_KEY et AUTH_EMAIL_FROM.");const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[input.to],subject:"Votre code de connexion Tridcast",html:renderLoginEmail(input)})});if(!response.ok)throw new Error("Le service d’email est temporairement indisponible.")}}

