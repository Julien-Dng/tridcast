import{describe,expect,it}from"vitest";import{assertStatusTransition,buildRenderPlan,realEstateInputSchema}from"../src/domain/video";import{assertOrganizationAccess}from"../src/server/auth/session";const input={vertical:"real_estate",objective:"property_presentation",language:"fr",content:{title:"Appartement lumineux",city:"Bordeaux",surfaceM2:60,rooms:4,bedrooms:3,features:["balcon"]},video:{format:"9:16",durationSeconds:30,templateId:"real-estate-modern",tone:"premium",voiceOver:false},callToAction:{enabled:false,mode:"none"}} as const;describe("verticale immobilier",()=>{it("valide une présentation et un CTA facultatif",()=>{expect(realEstateInputSchema.parse({...input,callToAction:undefined}).callToAction.enabled).toBe(false)});it("rejette une ville absente",()=>{expect(()=>realEstateInputSchema.parse({...input,content:{...input.content,city:""}})).toThrow()});it("construit un plan indépendant du fournisseur",()=>{const p=buildRenderPlan(input,[{id:"a1",position:0,primary:true}]);expect(p.scenes.map(s=>s.type)).toEqual(["intro","key_facts","features","brand_outro"]);expect(p.scenes[3]?.callToAction).toBeNull()});it("adapte les scènes à un objectif éditorial",()=>{expect(buildRenderPlan({...input,objective:"educational"}).scenes.some(s=>s.type==="editorial")).toBe(true)})});describe("sécurité et états",()=>{it("isole les organisations",()=>{expect(()=>assertOrganizationAccess({userId:"u",organizationId:"a",role:"member"},"b")).toThrow("Accès interdit")});it("refuse une transition illégale",()=>{expect(()=>assertStatusTransition("draft","completed")).toThrow();expect(()=>assertStatusTransition("failed","queued")).not.toThrow()})});
