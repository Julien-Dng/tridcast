import{test,expect}from"@playwright/test";test("parcours immobilier avec fournisseur mock",async({page})=>{await page.goto("/");await page.getByRole("link",{name:/Créer ma première/}).click();await expect(page.getByText("Créons votre prochaine vidéo")).toBeVisible();await page.getByRole("button",{name:/Continuer/}).click();await page.getByRole("button",{name:"Présentation d’un bien"}).click();await page.getByRole("button",{name:/Continuer/}).click();await page.getByLabel(/Titre/).fill("Maison avec jardin");await page.getByLabel(/Ville/).fill("Bordeaux");await expect(page.getByRole("button",{name:/Continuer/})).toBeEnabled()});
